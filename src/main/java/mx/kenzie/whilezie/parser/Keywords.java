package mx.kenzie.whilezie.parser;

public interface Keywords {

    String
        WHILE = "while",
        READ = "read",
        WRITE = "write",
        HEAD = "hd",
        TAIL = "tl",
        CONSTRUCT = "cons",
        NIL = "nil",
        IF = "if",
        ELSE = "else";

}
